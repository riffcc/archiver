---
description: Don't give up easily or change approaches needlessly
globs: ["**/*.rs"
alwaysApply: false
---
If you are presenting something to the user as working, MAKE SURE `cargo build` works.

If it's a codebase with tests, MAKE SURE `cargo test` works. Skipping these commands will not end well for you.