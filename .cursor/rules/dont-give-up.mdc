---
description: Don't give up easily or change approaches needlessly
globs: ["**/*.rs", "Cargo.toml", "Cargo.lock"]
---
You should always try and fix errors rather than rewriting or destorying large sections of code.

If you think something is broken, don't remove it! Read the documentation!

Grep case insensitive through `unified/target/doc` to find answers.